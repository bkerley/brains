<%= form_for(@hideout) do |f| %>
  <% if @hideout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hideout.errors.count, "error") %> prohibited this hideout from being saved:</h2>

      <ul>
      <% @hideout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
